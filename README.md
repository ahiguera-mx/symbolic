### SymPy
* A minimal working prototype that takes a Lagrangian (L) as input, computes the corresponding Hamiltonian (H) symbolically using SymPy, and wraps the transformation logic into a PyTorch-style interface




